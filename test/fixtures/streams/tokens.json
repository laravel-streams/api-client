{
    "name": "Tokens",
    "description": "Personal access tokens.",
    "fields": {
        "id": {
            "type": "uuid",
            "unique": true,
            "required": true,
            "config": {
                "default": true
            }
        },
        "token": {
            "type": "string",
            "unique": true,
            "required": true
        },
        "user": {
            "type": "relationship",
            "unique": true,
            "required": true,
            "config": {
                "related": "users"
            }
        }
    }
}
